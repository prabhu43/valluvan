# Core
python-dotenv==1.0.1
pandas==2.2.2
pyarrow==16.1.0

# Vector search / embeddings
qdrant-client==1.10.1
# fastembed 0.5+ adds the cross-encoder reranker (fastembed.rerank) used in
# rag/rerank.py. Pinned to the latest that ships cp313 wheels (onnxruntime 1.27).
fastembed==0.8.0

# LLM (OpenAI-compatible client; used for Groq/OpenAI/Ollama)
openai==2.46.0

# Interface
# 1.40.x accepts pillow 11.x (fastembed 0.8.0 requires pillow>=11); keeps the
# Docker image and local dev on the same Streamlit. Chat/AppTest APIs unchanged.
streamlit==1.40.2

# Monitoring
# 2.9.10+ ships cp313 wheels (2.9.9 has none -> source build fails on py3.13)
psycopg2-binary==2.9.12

# Evaluation / notebooks
scikit-learn==1.5.1
tqdm==4.66.4
jupyter==1.0.0
